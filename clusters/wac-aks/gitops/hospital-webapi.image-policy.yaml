apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: hospital-webapi
  namespace: wac-hospital
spec:
  imageRepositoryRef:
    name: hospital-webapi
  filterTags:
    pattern: "main.*"
  policy:
    alphabetical:
      order: asc
